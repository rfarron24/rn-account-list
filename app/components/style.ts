
import { StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  mainViewStyle : { borderWidth: 2, borderColor: 'grey' },
  textStyle : { padding: 10 },
  buttonContainerStyle: { width: '50%', padding: 10 }
});