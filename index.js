/**
 * @format
 */

import {AppRegistry} from 'react-native';
import MainPageScreen from './app/mainPageScreen'
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => MainPageScreen);
